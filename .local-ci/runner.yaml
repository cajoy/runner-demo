schema: local-ci/v1
source: auto

executors:
  local:
    driver: apple-container
    allowed_drivers: [apple-container, docker]

plugins:
  local:
    executable: runner-plugin-local

concurrency:
  max: 2
  on_battery: 1

store:
  path: auto

